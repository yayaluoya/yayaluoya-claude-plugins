{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "cache": true,
      "outputs": ["dist/**"]
    },
    "package": {
      "dependsOn": ["build", "^package"],
      "cache": false
    }
  }
}
