{
  "name": "@yayaluoya-claude-plugins/inject-system-proxy-plugin",
  "version": "0.0.1",
  "description": "读取系统代理配置并在会话启动时注入到环境变量",
  "type": "module",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/yayaluoya/yayaluoya-claude-plugins.git"
  },
  "scripts": {
    "build": "esbuild src/bin/inject-system-proxy.js --bundle --platform=node --format=esm --outfile=dist/inject-system-proxy.mjs",
    "test": "node test/inject-system-proxy.test.js",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
